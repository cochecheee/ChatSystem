name: Security

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  security:
    # Reusable workflow của chat-system. Pin theo tag khi v0.2.0 release.
    uses: cochecheee/sast-chat/.github/workflows/sast-ci.yml@main
    with:
      language: python
      dashboard_url: ${{ secrets.MCP_GATEWAY_URL }}
      pipeline_status: passed
    secrets:
      dashboard_token: ${{ secrets.MCP_WEBHOOK_TOKEN }}
