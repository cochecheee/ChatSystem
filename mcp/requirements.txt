# Core API
fastapi[all]
uvicorn[standard]

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
# Postgres async driver — used on Render production.
asyncpg>=0.30.0
# V3.6 — schema migrations. init_db() auto-runs `alembic upgrade head`
# on startup; tests skip this via SKIP_ALEMBIC=1 env.
alembic>=1.18.0
# MySQL async driver — used khi DATABASE_URL=mysql+asyncmy://...
# Local dev qua XAMPP (port 8888) hoặc managed MySQL.
asyncmy>=0.2.9

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP Client
httpx>=0.27.0

# SARIF Processing
sarif-pydantic>=0.6.2

# Security Analysis
cwe2>=3.0.0
cvss>=3.0.0
detect-secrets>=1.5.0
defusedxml>=0.7.1

# AI / LLM
google-genai>=1.73.1

# Auth
python-jose[cryptography]>=3.3.0
# bcrypt called directly (src/core/security.py); passlib 1.7.4 is broken
# against bcrypt>=4.1 (reads removed bcrypt.__about__), so we don't use it.
bcrypt>=4.0.0

# Rate Limiting
slowapi>=0.1.9

# V2.4 — Monitor + alert (Sentry is optional; SENTRY_DSN gates init)
sentry-sdk>=2.0.0

# V2.7 — Anthropic Model Context Protocol server (báo cáo tiến độ ch.3.2)
fastmcp>=3.3.0

# Utils
python-dotenv>=1.0.0
python-multipart>=0.0.9
pyyaml>=6.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
