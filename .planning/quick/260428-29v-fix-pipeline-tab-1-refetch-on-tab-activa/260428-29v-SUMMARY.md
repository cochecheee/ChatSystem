---
quick_id: 260428-29v
status: complete
date: 2026-04-28
commit: e9fc9e3
---

# Quick Task 260428-29v: Fix Pipeline Tab

## Goal
Fix pipeline tab: (1) refetch on tab activation to fix "No runs" stale state, (2) split pipelines into CI and CD workflow sections.

## What Was Done

`dashboard/src/pages/Pipelines.tsx` — 146 insertions, 38 deletions:

### Task 1: Refetch on Tab Activation
- Mount-time `useEffect` calls `api.github.runs()` directly in promise callbacks (no synchronous setState in effect body, satisfies `react-hooks/set-state-in-effect` ESLint rule)
- Added `fetchError` state shown in sidebar on failure
- Manual Refresh button uses `refresh()` helper that clears loading/error/runs before re-fetching
- Removed hardcoded `fetch('http://localhost:8000/...')` — now uses `api.github.runs()` which respects `VITE_API_URL`

### Task 2: CI/CD Split Sidebar
- Added `categorizeRun(run)` pure function using tokenized keyword matching (`CD_KEYWORDS = ['cd','deploy','release','publish','staging','production','prod']`) with substring fallback for compound names
- Added `SectionHeader` component with sticky positioning
- Sidebar now shows CI section + CD section, each with count badge, per-section `latest` badge, and empty-bucket placeholder

### Deviation
ESLint `react-hooks/set-state-in-effect` rule required inlining the initial fetch in `useEffect` rather than calling `refresh()` synchronously — fixed inline.

### Cleanup
- Removed debug `console.log('[DBG raw fetch]...')` 
- Removed orange DBG header div

## Task 3: Human Verify (checkpoint)
Visual verification of tab activation + CI/CD split requires manual browser testing.
- Navigate away from Pipelines tab and back — should now load runs immediately without manual refresh
- Sidebar should show two sections: CI Workflows and CD Workflows

## Commits
- `e9fc9e3`: feat(260428-29v-01): refetch on tab activation + CI/CD split sidebar
