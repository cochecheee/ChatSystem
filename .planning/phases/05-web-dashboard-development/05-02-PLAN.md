---
phase: 05-web-dashboard-development
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [dashboard/src/hooks/useFindings.ts, dashboard/src/hooks/usePipeline.ts, dashboard/src/components/security/FindingsTable.tsx, dashboard/src/components/security/FindingDetail.tsx]
autonomous: true
requirements: [REQ-04-01]
user_setup: []

must_haves:
  truths:
    - "Findings update automatically every 15 seconds"
    - "Pipeline status changes state (RUNNING, IDLE) in UI"
    - "Clicking a finding opens a detailed drawer view"
  artifacts:
    - path: "dashboard/src/hooks/useFindings.ts"
      provides: "TanStack Query polling logic"
    - path: "dashboard/src/components/security/FindingsTable.tsx"
      provides: "Main vulnerability view"
  key_links:
    - from: "dashboard/src/hooks/useFindings.ts"
      to: "/api/findings"
      via: "Polling fetch"
---

<objective>
Implement real-time data fetching for security findings and build the main vulnerability visualization components.

Purpose: Display the core data of the security system.
Output: Functioning polling mechanism and a responsive findings table.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-web-dashboard-development/05-01-SUMMARY.md
@.planning/phases/05-web-dashboard-development/05-CONTEXT.md
@.planning/phase-5/research/RESEARCH.md
</context>

<tasks>

<task type="auto" >
  <name>Task 1: Implement Dynamic Polling Hooks</name>
  <files>dashboard/src/hooks/useFindings.ts, dashboard/src/hooks/usePipeline.ts, dashboard/src/services/api.ts</files>
  <behavior>
    - `useFindings` should fetch data from `/api/findings`.
    - `usePipeline` should fetch from `/api/pipeline/latest`.
    - Polling interval must be 15s by default (D-06).
    - `useFindings` should use `usePipeline` state to adjust interval: 5s if 'RUNNING', 30s if 'IDLE'.
  </behavior>
  <action>
    - Create a basic API service/axios instance in `api.ts`.
    - Implement the TanStack Query hooks in `hooks/` directory.
    - Export hooks for use in components.
  </action>
  <verify>
    <automated>npm run test -- dashboard/src/hooks/useFindings.test.ts</automated>
  </verify>
  <done>Polling hooks implemented with dynamic intervals</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Findings List UI (Bento Grid / Table)</name>
  <files>dashboard/src/components/security/FindingsTable.tsx, dashboard/src/components/security/FindingCard.tsx</files>
  <action>
    - Use `FindingsTable.tsx` to display a list of all vulnerabilities.
    - Implement a `Bento Grid` style summary at the top for Critical findings (D-02).
    - Use `SeverityBadge` from Plan 01 for the severity column.
    - Add filtering by severity and scanner type.
  </action>
  <verify>
    <automated>ls dashboard/src/components/security/FindingsTable.tsx</automated>
  </verify>
  <done>Findings list UI built and rendering real data từ MCP Gateway</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement Finding Detail View</name>
  <files>dashboard/src/components/security/FindingDetail.tsx</files>
  <action>
    - Create a Drawer-based detail view (using Shadcn `drawer`).
    - Show full vulnerability description, CWE/OWASP categories, and CVSS score.
    - Provide an "Analyze with AI" button that triggers an event for the Chat Panel.
  </action>
  <verify>
    <automated>ls dashboard/src/components/security/FindingDetail.tsx</automated>
  </verify>
  <done>Detail view implemented with all metadata fields</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> UI | External data processed by hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Findings Data | mitigate | Ensure API only returns sanitized security metadata (D-06) |
</threat_model>

<verification>
Ensure findings list renders on the dashboard. Verify polling frequency using DevTools Network tab.
</verification>

<success_criteria>
- Findings list is populated and updates every 15s.
- Critical findings appear in the Bento Grid summary.
- Detail drawer opens with complete metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard-development/05-02-SUMMARY.md`
</output>
