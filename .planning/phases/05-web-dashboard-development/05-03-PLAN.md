---
phase: 05-web-dashboard-development
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: [dashboard/src/components/chat/ChatPanel.tsx, dashboard/src/components/chat/MessageBubble.tsx, dashboard/src/components/chat/CommandMenu.tsx, dashboard/src/hooks/useChat.ts]
autonomous: true
requirements: [REQ-04-02, REQ-04-03]
user_setup: []

must_haves:
  truths:
    - "Typing '/' in the chat input opens the command menu"
    - "AI messages are rendered with syntax-highlighted code blocks"
    - "Chat history is scrollable and maintains bottom focus on new messages"
    - "/status gọi GET /api/pipeline/status và hiện kết quả trong chat"
    - "/results gọi GET /api/findings và hiện summary trong chat"
  artifacts:
    - path: "dashboard/src/components/chat/ChatPanel.tsx"
      provides: "ChatOps UI container"
    - path: "dashboard/src/components/chat/CommandMenu.tsx"
      provides: "Slash command suggestion list"
  key_links:
    - from: "dashboard/src/components/chat/ChatPanel.tsx"
      to: "/api/chat/execute"
      via: "Post command"
---

<objective>
Build the interactive ChatOps interface with slash command suggestions and secure AI response rendering.

Purpose: Allow direct interaction with the AI security assistant.
Output: Feature-rich chat panel integrated into the dashboard.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-web-dashboard-development/05-01-SUMMARY.md
@.planning/phases/05-web-dashboard-development/05-CONTEXT.md
@.planning/phase-5/research/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build Chat Panel & Message Bubble UI</name>
  <files>dashboard/src/components/chat/ChatPanel.tsx, dashboard/src/components/chat/MessageBubble.tsx, dashboard/src/hooks/useChat.ts</files>
  <action>
    - Create a side panel (fixed or sliding) for `ChatPanel`.
    - Implement a `MessageBubble` that distinguishes between 'User' and 'AI' roles.
    - Implement `useChat` hook to manage local message state and scroll position.
    - Ensure message history persists during the session (D-07).
  </action>
  <verify>
    <automated>ls dashboard/src/components/chat/ChatPanel.tsx</automated>
  </verify>
  <done>Chat Panel UI built with scrolling message history</done>
</task>

<task type="auto" >
  <name>Task 2: Implement Slash Command Suggestion Menu</name>
  <files>dashboard/src/components/chat/CommandMenu.tsx, dashboard/src/components/chat/ChatInput.tsx</files>
  <behavior>
    - Inputting '/' should toggle the `CommandMenu`.
    - `CommandMenu` should show list of slash commands (/explain, /fix, /status, /scan, /results, /rerun, /approve, /report).
    - Selecting a command from the menu should populate the input field.
  </behavior>
  <action>
    - Use `cmdk` library to build the command menu (D-08).
    - Integrate with the chat input field.
    - Filter commands based on input after the slash.
  </action>
  <verify>
    <automated>npm run test -- dashboard/src/components/chat/CommandMenu.test.tsx</automated>
  </verify>
  <done>Slash command menu implemented with cmdk</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Handle /status và /results trong ChatPanel</name>
  <files>dashboard/src/components/chat/ChatPanel.tsx, dashboard/src/hooks/useChat.ts</files>
  <action>
    Trong `useChat.ts`, xử lý 2 lệnh read-only trực tiếp ở frontend:
    - `/status` → `GET /api/pipeline/status` → hiện message: "Pipeline: {status} | Last run: {timestamp}"
    - `/results` → `GET /api/findings` → hiện summary: "Tổng: {total} | Critical: {n} | High: {n} | ..."
    Các lệnh phức tạp hơn (/explain, /fix, /approve, /revoke, /scan, /rerun, /report) → gửi về `POST /api/chat/command` (Phase 6).
  </action>
  <verify>
    <automated>ls dashboard/src/hooks/useChat.ts</automated>
  </verify>
  <done>/status và /results trả về kết quả trong chat không cần round-trip phức tạp.</done>
</task>

<task type="auto">
  <name>Task 4: Secure Markdown Rendering & Syntax Highlighting</name>
  <files>dashboard/src/components/chat/MarkdownContent.tsx</files>
  <action>
    - Use `react-markdown` to render AI responses.
    - Apply `rehype-sanitize` for security (Pitfall 2).
    - Use `react-syntax-highlighter` for code blocks within markdown (D-04).
    - Support GFM (GitHub Flavored Markdown).
  </action>
  <verify>
    <automated>ls dashboard/src/components/chat/MarkdownContent.tsx</automated>
  </verify>
  <done>Markdown rendering implemented with security sanitization</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Response -> UI | Untrusted content from LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | AI Markdown | mitigate | Use rehype-sanitize and strict content parsing (Pitfall 2) |
</threat_model>

<verification>
Check chat input behavior with `/`. Verify code block rendering with sample AI markdown.
</verification>

<success_criteria>
- Command menu opens correctly.
- AI messages with code blocks are styled and highlighted.
- Chat input handles slash commands properly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard-development/05-03-SUMMARY.md`
</output>
