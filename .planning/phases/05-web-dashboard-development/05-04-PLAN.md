---
phase: 05-web-dashboard-development
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified: [dashboard/src/components/charts/SeverityChart.tsx, dashboard/src/components/stats/ScannerStats.tsx, dashboard/src/App.tsx]
autonomous: true
requirements: [REQ-04-04]
user_setup: []

must_haves:
  truths:
    - "A pie or bar chart displays the severity distribution"
    - "Scanner stats show counts for Semgrep, CodeQL, ESLint, etc."
    - "Dashboard layout correctly integrates Findings, Charts, and Chat"
  artifacts:
    - path: "dashboard/src/components/charts/SeverityChart.tsx"
      provides: "Visual overview of vulnerabilities"
  key_links:
    - from: "dashboard/src/components/charts/SeverityChart.tsx"
      to: "dashboard/src/hooks/useFindings.ts"
      via: "Data consumer"
---

<objective>
Implement security data visualizations and perform final dashboard assembly.

Purpose: Provide high-level insights and a cohesive user experience.
Output: Completed dashboard with interactive charts and integrated ChatOps.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-web-dashboard-development/05-02-SUMMARY.md
@.planning/phases/05-web-dashboard-development/05-03-SUMMARY.md
@.planning/phases/05-web-dashboard-development/05-CONTEXT.md
@.planning/phase-5/research/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Severity Distribution Chart</name>
  <files>dashboard/src/components/charts/SeverityChart.tsx</files>
  <action>
    - Implement a `Recharts` Pie Chart or Bar Chart showing the count of vulnerabilities by severity (D-09).
    - Use the same color palette as `SeverityBadge` for chart segments.
    - Make the chart responsive and interactive (show count on hover).
  </action>
  <verify>
    <automated>ls dashboard/src/components/charts/SeverityChart.tsx</automated>
  </verify>
  <done>Severity chart implemented with Recharts</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scanner Statistics Cards</name>
  <files>dashboard/src/components/stats/ScannerStats.tsx</files>
  <action>
    - Create a set of cards showing the number of findings detected by each scanner (Semgrep, CodeQL, etc.).
    - Display the "Pass/Fail" status of the last scan.
    - Use Shadcn `card` and `badge` primitives.
  </action>
  <verify>
    <automated>ls dashboard/src/components/stats/ScannerStats.tsx</automated>
  </verify>
  <done>Scanner statistics cards implemented</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final Dashboard Layout Wiring</name>
  <files>dashboard/src/App.tsx, dashboard/src/components/Layout.tsx</files>
  <action>
    - Update `App.tsx` to include the `QueryClientProvider`.
    - Arrange the `FindingsTable`, `SeverityChart`, `ScannerStats`, and `ChatPanel` in the `Layout`.
    - Ensure the Chat Panel remains accessible but doesn't obscure critical findings data.
    - Final CSS polish for spacing and consistency.
  </action>
  <verify>
    <automated>ls dashboard/src/App.tsx</automated>
  </verify>
  <done>Dashboard components wired and layout completed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | Chart data presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Stats Overload | mitigate | Filter irrelevant metrics to avoid alert fatigue (Pitfall 1) |
</threat_model>

<verification>
Verify all components render correctly in the main view. Check chart interactivity.
</verification>

<success_criteria>
- Severity chart correctly reflects the findings data.
- Dashboard layout is responsive and visually consistent.
- Chat Panel is integrated and functional within the layout.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard-development/05-04-SUMMARY.md`
</output>
