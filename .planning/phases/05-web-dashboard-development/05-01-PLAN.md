---
phase: 05-web-dashboard-development
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dashboard/package.json, dashboard/src/App.tsx, dashboard/src/index.css, dashboard/tailwind.config.ts]
autonomous: true
requirements: [REQ-04-01]
user_setup: []

must_haves:
  truths:
    - "Project has Shadcn/UI and Tailwind CSS 4 configured"
    - "Application shell renders with a side panel and main area"
    - "Severity badges render correctly for Critical, High, Medium, Low"
  artifacts:
    - path: "dashboard/src/components/ui"
      provides: "Shadcn/UI primitives"
    - path: "dashboard/src/components/security/SeverityBadge.tsx"
      provides: "Consistent severity visualization"
  key_links:
    - from: "dashboard/src/App.tsx"
      to: "dashboard/src/components/Layout.tsx"
      via: "Rendering core structure"
---

<objective>
Initialize the dashboard project foundation with Shadcn/UI, Tailwind CSS 4, and core layout components.

Purpose: Provide the visual and technical baseline for Phase 5.
Output: Configured React environment with core UI primitives and a responsive layout shell.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-web-dashboard-development/05-CONTEXT.md
@.planning/phase-5/research/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project Dependencies & Foundation</name>
  <files>dashboard/package.json, dashboard/src/index.css</files>
  <action>
    - Install necessary dependencies for Phase 5:
      - `@tanstack/react-query` (v5)
      - `lucide-react`, `clsx`, `tailwind-merge`
      - `recharts`, `react-markdown`, `rehype-sanitize`, `react-syntax-highlighter`
      - `cmdk`, `date-fns`
    - Configure Tailwind CSS 4 in `index.css` or `tailwind.config.ts` (per research recommendation).
    - Ensure Lucide icons are accessible.
  </action>
  <verify>
    <automated>cd dashboard && npm list @tanstack/react-query lucide-react tailwind-merge recharts</automated>
  </verify>
  <done>Dependencies installed, Tailwind 4 directives added to CSS</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Initialize Shadcn/UI & Core Theme</name>
  <files>dashboard/src/components/ui/*, dashboard/components.json</files>
  <action>
    - Initialize Shadcn/UI (using `npx shadcn-ui@latest init` or similar).
    - Add essential primitives: `button`, `card`, `badge`, `scroll-area`, `drawer`, `tabs`, `toast`.
    - Configure the core theme (Slate/Zinc with high-contrast accents for security severity per D-03).
    - Create a basic `Layout.tsx` shell with a sidebar for navigation and a main content area.
  </action>
  <verify>
    <automated>ls dashboard/src/components/ui/button.tsx</automated>
  </verify>
  <done>Shadcn primitives created, Layout component ready</done>
</task>

<task type="auto" >
  <name>Task 3: Create SeverityBadge & Shared Components</name>
  <files>dashboard/src/components/security/SeverityBadge.tsx, dashboard/src/components/common/LoadingSpinner.tsx</files>
  <behavior>
    - `SeverityBadge` must accept a `level` prop (CRITICAL, HIGH, MEDIUM, LOW, INFO).
    - CRITICAL must have a red background/border and white text (per RESEARCH.md mapping).
    - INFO must have a neutral/slate appearance.
  </behavior>
  <action>
    - Implement `SeverityBadge.tsx` using Tailwind classes from RESEARCH.md.
    - Implement a generic `LoadingSpinner.tsx` for state feedback.
    - Export components for use in subsequent plans.
  </action>
  <verify>
    <automated>npm run test -- dashboard/src/components/security/SeverityBadge.test.tsx</automated>
  </verify>
  <done>Visual components created and tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> UI | Internal layout consistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | UI Styling | mitigate | Use Tailwind built-in sanitization and consistent component primitives |
</threat_model>

<verification>
Check `dashboard/src/components/ui` exists. Ensure `npm run dev` starts without errors.
</verification>

<success_criteria>
- Dashboard shell renders.
- Shadcn components are usable.
- SeverityBadge correctly maps color to level.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard-development/05-01-SUMMARY.md`
</output>
