---
phase: 02-mcp-gateway
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mcp/src/services/github_client.py
  - mcp/src/core/guardrails.py
  - mcp/.env.example
autonomous: true
requirements: [REQ-2.1, REQ-2.3, REQ-2.4]
user_setup:
  - service: github
    why: "Artifact fetching"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub Settings -> Developer Settings -> Personal access tokens (classic or fine-grained)"
must_haves:
  truths:
    - "GitHub client downloads and extracts .sarif files từ real GitHub CI artifacts"
    - "Secrets (e.g., API keys) are masked in finding messages"
    - "PII (Emails/IPs) is masked in finding messages"
  artifacts:
    - path: "mcp/src/services/github_client.py"
      provides: "Artifact download and zip extraction logic"
    - path: "mcp/src/core/guardrails.py"
      provides: "Scrubbing and Injection prevention services"
  key_links:
    - from: "mcp/src/services/github_client.py"
      to: "GitHub REST API"
      via: "httpx async calls"
    - from: "mcp/src/core/guardrails.py"
      to: "detect-secrets library"
      via: "scrub_content method"
---

<objective>
Implement the GitHub Artifact Fetcher and the Security Guardrail layer (Scrubbing & Injection prevention).

Purpose: Securely ingest raw data from GitHub CI/CD and sanitize it for downstream AI analysis.
Output: Working GitHub client for artifact downloads and a robust sanitization service.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-2/research/RESEARCH.md
@.planning/phases/02-mcp-gateway-server-development/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async GitHub Client</name>
  <files>mcp/src/services/github_client.py, mcp/.env.example</files>
  <action>
    - Create `mcp/src/services/github_client.py` using `httpx`.
    - Implement `GitHubClient.fetch_artifact(owner, repo, artifact_id)` method.
    - Handle artifact downloading and extraction from ZIP (use `zipfile` and `io.BytesIO`).
    - Add logic to filter and return files relevant to security results (.sarif, .xml, .json).
    - Add `GITHUB_TOKEN` to `mcp/.env.example`.
  </action>
  <verify>
    <automated>pytest mcp/tests/test_github_client.py</automated>
  </verify>
  <done>Client can fetch, unzip, and read artifact files from GitHub API (mocked in tests).</done>
</task>

<task type="auto">
  <name>Task 2: PII & Secret Scrubbing Guardrail</name>
  <files>mcp/src/core/guardrails.py</files>
  <action>
    - Implement `ScrubbingService` in `mcp/src/core/guardrails.py` using `detect-secrets`.
    - Create a `scrub_content(content: str) -> str` method that replaces detected secrets and high-entropy strings with `[SECRET_SCRUBBED]`.
    - Implement PII removal for common patterns (Emails, IP Addresses) using regex.
    - Ensure the service is configurable via environment variables (enable/disable filters).
  </action>
  <verify>
    <automated>pytest mcp/tests/test_guardrails_scrubbing.py</automated>
  </verify>
  <done>Sensitive information is reliably removed/masked from input strings.</done>
</task>

<task type="auto">
  <name>Task 3: Prompt Injection Protection</name>
  <files>mcp/src/core/guardrails.py</files>
  <action>
    - Add `InjectionGuardrail` to `mcp/src/core/guardrails.py`.
    - Implement basic checks for known indirect prompt injection patterns in SAST findings (e.g., `<script>`, `System.exit`, known jailbreak attempts in rule messages).
    - Implement a "Length Limiter" and "Character White-listing" for dynamic finding data that will be passed to prompts.
  </action>
  <verify>
    <automated>pytest mcp/tests/test_guardrails_injection.py</automated>
  </verify>
  <done>Potential injection attempts in SAST findings are flagged or neutralized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → Client | Malicious artifacts (Zip Slip). |
| Scrubbed Data → AI | Incomplete scrubbing leaking PII/Secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Zip Extraction | mitigate | Sanitize extraction paths and limit extraction size to prevent Zip Slip / Zip Bomb attacks. |
| T-02-04 | Info Disclosure | Guardrails | mitigate | Multi-layered scrubbing (regex + `detect-secrets`). |
| T-02-05 | Tampering | Injection Guardrail | mitigate | Implement strict output encoding and delimiter-based prompting (Phase 3 task). |
</threat_model>

<verification>
1. Run `pytest mcp/tests/test_github_client.py`.
2. Run `pytest mcp/tests/test_guardrails_*.py`.
3. Verify that the `.env.example` file contains the `GITHUB_TOKEN` variable.
</verification>

<success_criteria>
- GitHub client successfully fetches and extracts artifacts.
- Secrets and PII are masked in text data.
- Basic prompt injection patterns are neutralized.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-gateway/02-02-SUMMARY.md`
</output>
