---
phase: 02-mcp-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/requirements.txt
  - mcp/src/main.py
  - mcp/src/core/db.py
  - mcp/src/models/entities.py
  - mcp/src/models/schemas.py
  - mcp/tests/conftest.py
autonomous: true
requirements: [REQ-5.1]
must_haves:
  truths:
    - "FastAPI server responds to health check"
    - "SQLite database is initialized with tables for Projects, Artifacts, and Findings"
    - "Pydantic models validate SARIF-compliant payloads"
  artifacts:
    - path: "mcp/src/main.py"
      provides: "Application entry point"
    - path: "mcp/src/core/db.py"
      provides: "Async SQLAlchemy engine and session management"
    - path: "mcp/src/models/entities.py"
      provides: "SQLAlchemy ORM models"
  key_links:
    - from: "mcp/src/main.py"
      to: "mcp/src/core/db.py"
      via: "startup event to initialize DB"
---

<objective>
Establish the core foundation of the MCP Gateway Server, including the FastAPI application structure, database layer (SQLAlchemy + SQLite), and unified data models.

Purpose: Provide the base infrastructure for all subsequent security processing and storage.
Output: Working FastAPI app with DB connectivity and SARIF-ready data models.
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-2/research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffold & Basic API</name>
  <files>mcp/requirements.txt, mcp/src/main.py, mcp/tests/conftest.py</files>
  <action>
    - Initialize `mcp/requirements.txt` with dependencies: fastapi, uvicorn, sqlalchemy, aiosqlite, pydantic, sarif-pydantic, httpx, python-dotenv, pytest, pytest-asyncio.
    - Create `mcp/src/main.py` with a basic FastAPI app, health check endpoint, and root route.
    - Set up `mcp/tests/conftest.py` with an async FastAPI test client fixture using `httpx`.
  </action>
  <verify>
    <automated>pytest mcp/tests/test_main.py</automated>
  </verify>
  <done>FastAPI app starts and responds to health check with 200 OK.</done>
</task>

<task type="auto">
  <name>Task 2: Database Layer & SQLAlchemy Models</name>
  <files>mcp/src/core/db.py, mcp/src/models/entities.py</files>
  <action>
    - Implement async SQLAlchemy engine and session management in `mcp/src/core/db.py` using `aiosqlite`.
    - Define SQLAlchemy entities in `mcp/src/models/entities.py`:
        - `Project`: id, name, github_url.
        - `Artifact`: id, github_artifact_id, project_id, status (pending/processed/failed).
        - `Finding`: id, artifact_id, tool, rule_id, severity, message, file_path, line_number, raw_data (JSON), normalized_at, cwe_id, cvss_score.
    - Add a function to initialize the database (create tables) in `mcp/src/core/db.py`.
  </action>
  <verify>
    <automated>pytest mcp/tests/test_db.py</automated>
  </verify>
  <done>SQLite database initializes correctly and models can be queried via async sessions.</done>
</task>

<task type="auto">
  <name>Task 3: Unified Data Schemas (Pydantic)</name>
  <files>mcp/src/models/schemas.py</files>
  <action>
    - Define Pydantic models in `mcp/src/models/schemas.py` for API requests and internal data transfer.
    - Import `SarifLog` from `sarif_pydantic` as the base for raw input validation.
    - Create `FindingCreate` and `FindingOut` schemas that map to the database entities.
    - Implement a "Deduplication Hash" utility function (e.g., hash of rule_id + file_path + message) to be used for future deduplication logic.
  </action>
  <verify>
    <automated>pytest mcp/tests/test_schemas.py</automated>
  </verify>
  <done>Pydantic models validate SARIF-like payloads and provide clean interfaces for the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External → API | Untrusted JSON/XML payloads from GitHub or Dashboard. |
| API → SQLite | Potential SQL Injection through malicious input in models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | Database | mitigate | Use SQLAlchemy ORM and parameterized queries (Pydantic validation at entry). |
| T-02-02 | Info Disclosure | API | mitigate | Hide DB connection strings and secrets using `python-dotenv` and separate config. |
</threat_model>

<verification>
1. Run `pytest mcp/tests/` to ensure all foundation components (Main, DB, Schemas) work together.
2. Verify `mcp.db` is created and has the correct schema after running the initialization task.
</verification>

<success_criteria>
- FastAPI server is operational with health check.
- Database models for Projects, Artifacts, and Findings are defined and tables created.
- Pydantic schemas validate input data correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-gateway/02-01-SUMMARY.md`
</output>
