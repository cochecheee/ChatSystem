---
phase: 03-llm-orchestrator-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/requirements.txt
  - mcp/src/services/llm/client.py
  - mcp/tests/services/llm/conftest.py
  - mcp/.env.example
autonomous: true
requirements: [REQ-3.1]
user_setup:
  - service: Gemini API
    why: "Access to Gemini for vulnerability analysis"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> Get API Key"
      - name: GEMINI_MODEL
        source: "Tuỳ chọn: gemini-2.5-flash (default), gemini-2.5-pro, v.v."

must_haves:
  truths:
    - "google-genai SDK (không phải google-generativeai) được cài đặt"
    - "GeminiClient có thể khởi tạo với API key từ env var"
    - "Model name đọc từ GEMINI_MODEL env var, default gemini-2.5-flash"
    - "Mocking infrastructure cho pytest tồn tại"
  artifacts:
    - path: "mcp/src/services/llm/client.py"
      provides: "Gemini API client wrapper (google-genai SDK)"
    - path: "mcp/tests/services/llm/conftest.py"
      provides: "Pytest fixtures for mocking Gemini responses"
  key_links:
    - from: "mcp/src/services/llm/client.py"
      to: "google.genai"
      via: "genai.Client initialization"
---

<objective>
Thiết lập nền tảng cho LLM Orchestrator: cài đặt SDK `google-genai` (mới, thay thế deprecated `google-generativeai`), cấu hình GeminiClient với model selection linh hoạt qua env var, và xây dựng mocking infrastructure cho pytest.

Purpose: Giao tiếp ổn định với Gemini API thật (dev + prod). Mocking chỉ dùng trong pytest — KHÔNG dùng mock khi chạy server local.
Output: GeminiClient wrapper (dùng Gemini thật) và Mocking fixtures (chỉ cho tests).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-3/research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cài đặt Dependencies</name>
  <files>mcp/requirements.txt, mcp/.env.example</files>
  <action>
    - Đảm bảo `google-genai>=1.73.1` có trong requirements.txt (KHÔNG dùng google-generativeai).
    - Đảm bảo `slowapi>=0.1.9` có trong requirements.txt cho rate limiting.
    - Thêm vào `mcp/.env.example`:
      ```
      GEMINI_API_KEY=your_key_here
      GEMINI_MODEL=gemini-2.5-flash
      GEMINI_MAX_RETRIES=3
      GEMINI_RATE_LIMIT=10/minute
      ```
    - Chạy `pip install -r requirements.txt`.
  </action>
  <verify>
    <automated>python -c "from google import genai; print('google-genai OK')"</automated>
  </verify>
  <done>google-genai SDK cài đặt thành công.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai GeminiClient Wrapper</name>
  <files>mcp/src/services/llm/client.py</files>
  <action>
    - Tạo `mcp/src/services/llm/client.py`.
    - Implement class `GeminiClient` sử dụng `google-genai` SDK:
      ```python
      from google import genai
      from google.genai import types

      class GeminiClient:
          def __init__(self):
              self.client = genai.Client(api_key=os.getenv("GEMINI_API_KEY"))
              self.model = os.getenv("GEMINI_MODEL", "gemini-2.5-flash")
              self.max_retries = int(os.getenv("GEMINI_MAX_RETRIES", "3"))

          async def generate(self, prompt: str, system_instruction: str,
                             response_schema: type) -> str:
              # Implement với retry + exponential backoff
              ...
      ```
    - Implement exponential backoff retry (1s → 2s → 4s) cho lỗi 429/503.
    - Log mỗi lần retry với warning level.
  </action>
  <verify>
    <automated>python -c "from mcp.src.services.llm.client import GeminiClient; print('GeminiClient loaded')"</automated>
  </verify>
  <done>GeminiClient có thể import, khởi tạo, và retry khi gặp lỗi quota.</done>
</task>

<task type="auto">
  <name>Task 3: Mocking Infrastructure cho Pytest</name>
  <files>mcp/tests/services/llm/conftest.py</files>
  <action>
    - Tạo `mcp/tests/services/llm/conftest.py`.
    - Tạo fixture `mock_gemini_client` dùng `unittest.mock.AsyncMock` để override `GeminiClient.generate`.
    - Fixture phải hỗ trợ:
      1. Return JSON string hợp lệ (success case).
      2. Raise exception giả lập 429 (rate limit case).
      3. Return JSON string sai schema (validation failure case).
    - Tạo sample `AnalysisOutput` JSON fixture để tái sử dụng trong các tests sau.
  </action>
  <verify>
    <automated>pytest mcp/tests/services/llm/ -v</automated>
  </verify>
  <done>Mock fixtures hoạt động, pytest không có lỗi import.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | client.py | mitigate | API Key chỉ đọc từ env var, không hardcode, không log. |
| T-03-01-02 | Tampering | client.py | mitigate | Pin SDK version trong requirements.txt để tránh breaking changes. |
</threat_model>

<success_criteria>
1. `google-genai` (không phải `google-generativeai`) được cài đặt.
2. `GeminiClient` import được, model đọc từ `GEMINI_MODEL` env var.
3. Retry logic hoạt động với exponential backoff.
4. Mock fixtures sẵn sàng cho các tests ở plans sau.
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-orchestrator-integration/03-01-SUMMARY.md`
</output>
