---
phase: 01-ui-ux-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/tokens.css
  - dashboard/src/components/AlertBanner.tsx
  - dashboard/src/components/Badge.tsx
  - dashboard/src/components/StatusDot.tsx
  - dashboard/src/hooks/useAsyncAction.ts
  - dashboard/src/App.tsx
  - dashboard/src/utils/toast.ts
  - dashboard/src/components/Shell.tsx
  - dashboard/package.json
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "No toast or popup appears on any page when actions occur or background poll fires"
    - "AlertBanner component exists and renders inline error/success/warning/info states"
    - "Topbar bell icon shows a count badge when new critical/high findings appear"
    - "All --surface-1 and --surface-2 tokens resolve to real values (no transparent fallback)"
    - "Inter font loads and applies to all body text via --font-sans"
  artifacts:
    - path: "dashboard/src/components/AlertBanner.tsx"
      provides: "Inline feedback banner component"
      exports: ["AlertBanner"]
    - path: "dashboard/src/components/Badge.tsx"
      provides: "Typed chip/badge wrapper over .chip CSS class"
      exports: ["Badge"]
    - path: "dashboard/src/components/StatusDot.tsx"
      provides: "Colored status dot with optional label"
      exports: ["StatusDot"]
    - path: "dashboard/src/hooks/useAsyncAction.ts"
      provides: "Reusable async action state hook"
      exports: ["useAsyncAction"]
    - path: "dashboard/src/tokens.css"
      provides: "Design token source of truth + --surface-* aliases + new utility classes"
      contains: "--surface-1: var(--bg-elev)"
    - path: "dashboard/src/App.tsx"
      provides: "Root app without Toaster or notify imports"
      contains: "newCritHighCount"
    - path: "dashboard/src/components/Shell.tsx"
      provides: "Topbar with notif-dot badge support"
      contains: "newCritHighCount"
  key_links:
    - from: "dashboard/src/App.tsx"
      to: "dashboard/src/components/Shell.tsx (Topbar)"
      via: "newCritHighCount prop"
      pattern: "newCritHighCount"
    - from: "dashboard/src/pages/Chat.tsx (future 01-02)"
      to: "dashboard/src/components/AlertBanner.tsx"
      via: "import + JSX render"
      pattern: "AlertBanner"
---

<objective>
Build the design system foundation for Phase 1: define missing CSS tokens, create three
reusable components (AlertBanner, Badge, StatusDot), create the useAsyncAction hook, then
remove the sonner toast library from App.tsx and Shell.tsx — replacing the single
background-poll toast with a Topbar notification badge.

Purpose: All subsequent page cleanup tasks in plan 01-02 depend on AlertBanner and
useAsyncAction existing first. This plan is prerequisite infrastructure.

Output:
- dashboard/src/tokens.css — two new token aliases (--surface-1, --surface-2) + two new
  utility CSS classes (.alert-banner, .notif-dot)
- dashboard/src/components/AlertBanner.tsx — new component
- dashboard/src/components/Badge.tsx — new component
- dashboard/src/components/StatusDot.tsx — new component
- dashboard/src/hooks/useAsyncAction.ts — new hook
- dashboard/src/App.tsx — Toaster removed, notify removed, newCritHighCount state added
- dashboard/src/components/Shell.tsx — Topbar extended with newCritHighCount badge
- dashboard/src/utils/toast.ts — deleted
- dashboard/package.json — sonner removed from dependencies
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.claude/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/School/DoAnTotNghiep/chat-system/.planning/ROADMAP.md
@D:/School/DoAnTotNghiep/chat-system/.planning/phases/01-ui-ux-overhaul/01-RESEARCH.md
@D:/School/DoAnTotNghiep/chat-system/.planning/phases/01-ui-ux-overhaul/01-PATTERNS.md

<interfaces>
<!-- Key patterns extracted from the codebase. Use these directly. -->

From dashboard/src/tokens.css (lines 6–68, :root block):
- Insert after line 67 (end of :root closing brace), before dark theme block at line 70:
  --surface-1: var(--bg-elev);
  --surface-2: var(--bg-muted);

From dashboard/src/tokens.css (end of file, after line 999):
  .alert-banner { display: flex; align-items: center; gap: 10px; padding: 8px 12px; border-radius: var(--r-2); font-size: var(--ts-sm); margin-top: 8px; }
  .alert-banner-msg { flex: 1; }
  .notif-dot { position: absolute; top: 2px; right: 2px; width: 16px; height: 16px; border-radius: 50%; background: var(--err-fg); color: white; font-size: 9px; font-weight: 700; display: grid; place-items: center; border: 2px solid var(--bg); }

From dashboard/src/components/Shell.tsx (lines 84–89, TopbarProps):
  interface TopbarProps {
    active: PageId;
    onNav: (id: PageId) => void;
    theme: string;
    onToggleTheme: () => void;
  }

From dashboard/src/App.tsx (lines 1–11, current imports to remove):
  import { Toaster } from 'sonner';           // line 2 — DELETE
  import { notify, updateCritHighBaseline } from './utils/toast';  // line 11 — DELETE

From dashboard/src/App.tsx (lines 25–38, poll logic to rewrite):
  useEffect(() => {
    const fetch = () => {
      api.findings.list({ limit: 200 }).then(f => {
        setVulnCount(f.length);
        const critHigh = f.filter(x => x.severity === 'critical' || x.severity === 'high').length;
        notify.newFindings(critHigh);                        // REMOVE
        if (critHighRef.current === 0) updateCritHighBaseline(critHigh); // REMOVE
        critHighRef.current = critHigh;
      }).catch(() => {});
    };
    ...

From dashboard/src/App.tsx (lines 61–76, JSX to modify):
  <Topbar active={active} onNav={onNav} theme={theme} onToggleTheme={...} />
  ...
  <Toaster position="bottom-right" richColors />   // DELETE

From dashboard/src/components/Shell.tsx (lines 109–111, bell button to update):
  <button className="btn ghost" style={{ padding: 6 }}>
    <Icon name="bell" size={15} />
  </button>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add CSS token aliases and utility classes to tokens.css</name>
  <files>dashboard/src/tokens.css</files>

  <read_first>
    - dashboard/src/tokens.css — full file; know the :root block end (line 68) and file end (line 999) before editing
  </read_first>

  <action>
Make two targeted edits to dashboard/src/tokens.css:

EDIT 1 — Insert --surface-1 and --surface-2 aliases inside the :root { } block.
Find the closing brace of :root (the line that reads just `}` after `--shadow-pop`).
Insert these two lines immediately BEFORE that closing brace:

  /* Aliases — resolve undefined --surface-* tokens used in components */
  --surface-1: var(--bg-elev);   /* elevated surface = white/dark card */
  --surface-2: var(--bg-muted);  /* sunken input background */

EDIT 2 — Append new utility classes at the very end of tokens.css (after the last line):

/* ============================================================
   AlertBanner — inline feedback component (replaces sonner toasts)
   ============================================================ */
.alert-banner {
  display: flex; align-items: center; gap: 10px;
  padding: 8px 12px;
  border-radius: var(--r-2);
  font-size: var(--ts-sm);
  margin-top: 8px;
}
.alert-banner-msg { flex: 1; }

/* Notification dot — topbar badge for background poll results */
.notif-dot {
  position: absolute; top: 2px; right: 2px;
  width: 16px; height: 16px; border-radius: 50%;
  background: var(--err-fg); color: white;
  font-size: 9px; font-weight: 700;
  display: grid; place-items: center;
  border: 2px solid var(--bg);
}

Do NOT touch any other lines. Do NOT add any hex color values.
  </action>

  <verify>
    <automated>grep "--surface-1: var(--bg-elev)" D:/School/DoAnTotNghiep/chat-system/dashboard/src/tokens.css && grep "\.alert-banner {" D:/School/DoAnTotNghiep/chat-system/dashboard/src/tokens.css && grep "\.notif-dot {" D:/School/DoAnTotNghiep/chat-system/dashboard/src/tokens.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep "--surface-1: var(--bg-elev)" dashboard/src/tokens.css` returns a match
    - `grep "--surface-2: var(--bg-muted)" dashboard/src/tokens.css` returns a match
    - `grep "\.alert-banner {" dashboard/src/tokens.css` returns a match
    - `grep "\.notif-dot {" dashboard/src/tokens.css` returns a match
    - `grep "#[0-9A-Fa-f]\{3,6\}" dashboard/src/tokens.css` count is unchanged (no new hex values added)
    - `grep -r "@import\|fonts.googleapis\|font-face\|Inter" dashboard/index.html dashboard/src/index.css dashboard/src/tokens.css 2>/dev/null` returns at least one match confirming Inter font is loaded; if no match found, add `<link rel="preconnect" href="https://fonts.googleapis.com"><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">` to `dashboard/index.html` before closing `</head>`
  </acceptance_criteria>

  <done>tokens.css has --surface-1 and --surface-2 aliases inside :root, plus .alert-banner and .notif-dot utility classes appended at end of file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create AlertBanner, Badge, and StatusDot components + useAsyncAction hook</name>
  <files>
    dashboard/src/components/AlertBanner.tsx
    dashboard/src/components/Badge.tsx
    dashboard/src/components/StatusDot.tsx
    dashboard/src/hooks/useAsyncAction.ts
  </files>

  <read_first>
    - dashboard/src/tokens.css — confirm token names (--ok-fg, --ok-bg, --err-fg, --err-bg, --warn-fg, --warn-bg, --fg-2, --bg-muted, --ts-xs, --ts-sm) before writing components
    - dashboard/src/components/Icon.tsx — copy the React import style (import React from 'react' pattern)
    - dashboard/src/pages/Settings.tsx (lines 19–82) — confirms the useAsyncAction try/catch/finally shape
  </read_first>

  <action>
Create four new files using the exact signatures from the pattern map.

--- dashboard/src/components/AlertBanner.tsx ---

import React from 'react';

interface AlertBannerProps {
  type: 'error' | 'success' | 'info' | 'warning';
  message: string;
  onDismiss?: () => void;
  action?: { label: string; onClick: () => void };
}

export function AlertBanner({ type, message, onDismiss, action }: AlertBannerProps) {
  const vars = {
    error:   { fg: 'var(--err-fg)',  bg: 'var(--err-bg)'  },
    success: { fg: 'var(--ok-fg)',   bg: 'var(--ok-bg)'   },
    warning: { fg: 'var(--warn-fg)', bg: 'var(--warn-bg)' },
    info:    { fg: 'var(--fg-2)',    bg: 'var(--bg-muted)' },
  }[type];
  return (
    <div className="alert-banner" style={{ color: vars.fg, background: vars.bg }}>
      <span className="alert-banner-msg">{message}</span>
      {action && (
        <button className="btn ghost sm" onClick={action.onClick}>{action.label}</button>
      )}
      {onDismiss && (
        <button className="btn ghost sm" onClick={onDismiss}>×</button>
      )}
    </div>
  );
}

--- dashboard/src/components/Badge.tsx ---

import React from 'react';

type Severity = 'critical' | 'high' | 'medium' | 'low' | 'info';
type StatusVariant = 'passed' | 'failed' | 'running' | 'queued' | 'warning';

interface BadgeProps {
  variant: Severity | StatusVariant | 'neutral';
  dot?: boolean;
  children: React.ReactNode;
}

export function Badge({ variant, dot = false, children }: BadgeProps) {
  const isSev = ['critical', 'high', 'medium', 'low', 'info'].includes(variant);
  const cls = isSev
    ? `sev-${variant}`
    : variant === 'neutral' ? '' : `status-${variant}`;
  return (
    <span className={`chip${dot ? ' dot' : ''} ${cls}`.trim()}>
      {children}
    </span>
  );
}

--- dashboard/src/components/StatusDot.tsx ---

import React from 'react';

interface StatusDotProps {
  status: 'ok' | 'error' | 'warn' | 'info' | 'critical' | 'high' | 'medium' | 'low';
  label?: string;
  size?: number;
}

export function StatusDot({ status, label, size }: StatusDotProps) {
  const isSev = ['critical', 'high', 'medium', 'low', 'info'].includes(status);
  const inlineColor = !isSev ? ({
    ok:    'var(--ok-fg)',
    error: 'var(--err-fg)',
    warn:  'var(--warn-fg)',
    info:  'var(--fg-4)',
  } as Record<string, string>)[status] : undefined;

  return (
    <span style={{ display: 'inline-flex', alignItems: 'center', gap: 6 }}>
      {isSev
        ? <span className={`sev-dot ${status}`} style={size ? { width: size, height: size } : undefined} />
        : <span style={{ width: size ?? 8, height: size ?? 8, borderRadius: '50%', background: inlineColor, flexShrink: 0 }} />
      }
      {label && <span style={{ fontSize: 'var(--ts-xs)', color: 'var(--fg-3)' }}>{label}</span>}
    </span>
  );
}

--- dashboard/src/hooks/useAsyncAction.ts ---

Create the directory dashboard/src/hooks/ if it does not exist, then create:

import { useCallback, useState } from 'react';

interface AsyncState {
  loading: boolean;
  error: string | null;
  success: boolean;
}

export function useAsyncAction<T extends unknown[]>(
  fn: (...args: T) => Promise<void>
) {
  const [state, setState] = useState<AsyncState>({
    loading: false, error: null, success: false,
  });

  const run = useCallback(async (...args: T) => {
    setState({ loading: true, error: null, success: false });
    try {
      await fn(...args);
      setState({ loading: false, error: null, success: true });
    } catch (e) {
      setState({ loading: false, error: String(e), success: false });
    }
  }, [fn]);

  const clear = useCallback(() =>
    setState({ loading: false, error: null, success: false }), []);

  return { ...state, run, clear };
}

No external imports. No hex color values in any file.
  </action>

  <verify>
    <automated>cd D:/School/DoAnTotNghiep/chat-system/dashboard && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `dashboard/src/components/AlertBanner.tsx` exists and exports `AlertBanner`
    - `dashboard/src/components/Badge.tsx` exists and exports `Badge`
    - `dashboard/src/components/StatusDot.tsx` exists and exports `StatusDot`
    - `dashboard/src/hooks/useAsyncAction.ts` exists and exports `useAsyncAction`
    - `grep "export function AlertBanner" dashboard/src/components/AlertBanner.tsx` returns a match
    - `grep "export function useAsyncAction" dashboard/src/hooks/useAsyncAction.ts` returns a match
    - `cd dashboard && npx tsc --noEmit` exits 0 (no TypeScript errors in new files)
  </acceptance_criteria>

  <done>Four new files created (AlertBanner, Badge, StatusDot, useAsyncAction) with no TypeScript errors and no hex color literals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove sonner — delete toast.ts, update App.tsx and Shell.tsx, remove from package.json</name>
  <files>
    dashboard/src/App.tsx
    dashboard/src/utils/toast.ts
    dashboard/src/components/Shell.tsx
    dashboard/package.json
  </files>

  <read_first>
    - dashboard/src/App.tsx — read full file; know exact line numbers before editing
    - dashboard/src/utils/toast.ts — confirm all exports before deleting (notify, updateCritHighBaseline)
    - dashboard/src/components/Shell.tsx — read full file; know TopbarProps interface and bell button location
    - dashboard/package.json — confirm "sonner" is in dependencies before removing
  </read_first>

  <action>
Four targeted changes:

CHANGE 1 — Delete dashboard/src/utils/toast.ts entirely.
(The file is 31 lines. No other file should import from it after this task.)

CHANGE 2 — Rewrite dashboard/src/App.tsx:
a) Delete line 2: `import { Toaster } from 'sonner';`
b) Delete line 11: `import { notify, updateCritHighBaseline } from './utils/toast';`
c) Add new state variable after line 17 (after `const [vulnCount, setVulnCount] = useState(0);`):
   `const [newCritHighCount, setNewCritHighCount] = useState(0);`
d) Replace the contents of the poll useEffect (lines 25–38). The new version:

  useEffect(() => {
    const fetchData = () => {
      api.findings.list({ limit: 200 }).then(f => {
        setVulnCount(f.length);
        const critHigh = f.filter(x => x.severity === 'critical' || x.severity === 'high').length;
        if (critHighRef.current !== 0 && critHigh > critHighRef.current) {
          setNewCritHighCount(prev => prev + (critHigh - critHighRef.current));
        }
        critHighRef.current = critHigh;
      }).catch(() => {});
    };
    fetchData();
    const id = setInterval(fetchData, 60_000);
    return () => clearInterval(id);
  }, []);

e) Update the <Topbar> JSX to pass the two new props:
   BEFORE: `<Topbar active={active} onNav={onNav} theme={theme} onToggleTheme={() => setTheme(t => t === 'dark' ? 'light' : 'dark')} />`
   AFTER:  `<Topbar active={active} onNav={onNav} theme={theme} onToggleTheme={() => setTheme(t => t === 'dark' ? 'light' : 'dark')} newCritHighCount={newCritHighCount} onClearCritHigh={() => setNewCritHighCount(0)} />`

f) Delete the line: `<Toaster position="bottom-right" richColors />`

CHANGE 3 — Update dashboard/src/components/Shell.tsx:
a) Extend TopbarProps interface (lines 84–89) — add two optional props:
   BEFORE:
     interface TopbarProps {
       active: PageId;
       onNav: (id: PageId) => void;
       theme: string;
       onToggleTheme: () => void;
     }
   AFTER:
     interface TopbarProps {
       active: PageId;
       onNav: (id: PageId) => void;
       theme: string;
       onToggleTheme: () => void;
       newCritHighCount?: number;
       onClearCritHigh?: () => void;
     }

b) Update the Topbar function signature to destructure the new props:
   BEFORE: `export function Topbar({ active, onNav, theme, onToggleTheme }: TopbarProps) {`
   AFTER:  `export function Topbar({ active, onNav, theme, onToggleTheme, newCritHighCount, onClearCritHigh }: TopbarProps) {`

c) Replace the bell button (lines 109–111):
   BEFORE:
     <button className="btn ghost" style={{ padding: 6 }}>
       <Icon name="bell" size={15} />
     </button>
   AFTER:
     <button className="btn ghost" style={{ padding: 6, position: 'relative' }} onClick={onClearCritHigh}>
       <Icon name="bell" size={15} />
       {(newCritHighCount ?? 0) > 0 && (
         <span className="notif-dot">{newCritHighCount}</span>
       )}
     </button>

CHANGE 4 — Remove sonner from dashboard/package.json:
   In the "dependencies" object, delete the line: `"sonner": "^2.0.7"`
   Then run: cd dashboard && npm install
   (This cleans the lockfile.)
  </action>

  <verify>
    <automated>cd D:/School/DoAnTotNghiep/chat-system/dashboard && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `dashboard/src/utils/toast.ts` does NOT exist (file deleted)
    - `grep -r "from 'sonner'\|from \"sonner\"\|Toaster\|notify\.\|updateCritHighBaseline" dashboard/src/ --include="*.tsx" --include="*.ts"` returns zero matches
    - `grep "newCritHighCount" dashboard/src/App.tsx` returns a match (state declared)
    - `grep "newCritHighCount" dashboard/src/components/Shell.tsx` returns a match (prop used)
    - `grep "sonner" dashboard/package.json` returns zero matches
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>

  <done>sonner library fully removed from the codebase: toast.ts deleted, App.tsx poll replaced with local state, Shell.tsx Topbar shows a .notif-dot badge, build passes with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component state → UI | Local React state drives banner visibility; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | AlertBanner (message prop) | accept | Messages come from internal API error strings; no PII surfaces in Phase 1 error messages; can be tightened in Phase 4 when user-facing commands are repaired |
</threat_model>

<verification>
Run from dashboard/ directory:

```bash
# 1. No toast/sonner imports remain
grep -r "from 'sonner'\|Toaster\|notify\.\|updateCritHighBaseline" dashboard/src/ --include="*.tsx" --include="*.ts"
# Expected: zero matches

# 2. --surface tokens defined
grep --surface-1 dashboard/src/tokens.css
# Expected: one match

# 3. New components exist
ls dashboard/src/components/AlertBanner.tsx dashboard/src/components/Badge.tsx dashboard/src/components/StatusDot.tsx dashboard/src/hooks/useAsyncAction.ts
# Expected: all four files listed

# 4. TypeScript + build clean
cd dashboard && npm run build
# Expected: exits 0
```
</verification>

<success_criteria>
- All sonner references gone from the codebase (grep returns 0)
- dashboard/src/utils/toast.ts deleted
- sonner removed from package.json
- --surface-1 and --surface-2 defined in tokens.css :root block
- .alert-banner and .notif-dot CSS classes in tokens.css
- AlertBanner.tsx, Badge.tsx, StatusDot.tsx, useAsyncAction.ts created with correct exports
- App.tsx passes newCritHighCount to Topbar; no Toaster in JSX
- Shell.tsx Topbar shows .notif-dot when newCritHighCount > 0
- `cd dashboard && npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-overhaul/01-01-SUMMARY.md` using the
summary template. Include: files modified, what was created, decisions made, and any
deviations from the plan.
</output>
