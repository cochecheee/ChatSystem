---
phase: 01-ui-ux-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - dashboard/src/pages/Chat.tsx
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/Vulns.tsx
  - dashboard/src/pages/Pipelines.tsx
  - dashboard/src/pages/Reports.tsx
  - dashboard/src/pages/Settings.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Chat.tsx has zero notify.* calls; all feedback shown via AlertBanner or local state"
    - "Chat.tsx LoginOverlay uses var(--bg-elev) and var(--bg-muted) instead of var(--surface-1) and var(--surface-2)"
    - "Reports.tsx download error shown via AlertBanner, not an inline style div"
    - "Settings.tsx AddProjectForm error shown via AlertBanner; health banner uses StatusDot + Badge"
    - "No var(--surface-1) usage remains in any .tsx file; var(--surface-2) references in Pipelines.tsx and Reports.tsx resolve correctly via the alias defined in 01-01"
    - "All 5 pages pass TypeScript build with zero errors"
  artifacts:
    - path: "dashboard/src/pages/Chat.tsx"
      provides: "Chat page with AlertBanner for command and report feedback"
      contains: "AlertBanner"
    - path: "dashboard/src/pages/Reports.tsx"
      provides: "Reports page with AlertBanner for download error"
      contains: "AlertBanner"
    - path: "dashboard/src/pages/Settings.tsx"
      provides: "Settings page with AlertBanner for form error; StatusDot + Badge in health banner"
      contains: "AlertBanner"
  key_links:
    - from: "dashboard/src/pages/Chat.tsx (executeCommand, handleReport, handleApproveConfirm)"
      to: "dashboard/src/components/AlertBanner.tsx"
      via: "cmdStatus and reportStatus local state → AlertBanner render"
      pattern: "cmdStatus|reportStatus"
    - from: "dashboard/src/pages/Reports.tsx"
      to: "dashboard/src/components/AlertBanner.tsx"
      via: "downloadError state → AlertBanner render"
      pattern: "downloadError.*AlertBanner"
    - from: "dashboard/src/pages/Settings.tsx"
      to: "dashboard/src/components/StatusDot|Badge"
      via: "health state → StatusDot + Badge in health banner"
      pattern: "StatusDot|Badge"
---

<objective>
Page cleanup sweep across all 6 pages: replace Chat.tsx notify.* calls with AlertBanner,
migrate inline --surface-* token references to the aliased tokens (now safe because 01-01
defined them), upgrade per-page error divs to AlertBanner, and use StatusDot + Badge in
Settings health banner.

Purpose: 01-01 built the infrastructure. This plan wires it into every page to complete
UI-01 (zero toasts) and UI-03 (no unused/redundant UI elements, consistent presentation).

Output:
- Chat.tsx: notify import removed; 7 call sites replaced with local state + AlertBanner
- Overview.tsx: verify no notify calls; confirm page-header uses correct CSS classes
- Vulns.tsx: replace SevChip with Badge component; verify no surface token issues
- Pipelines.tsx: token fix self-resolves from 01-01; audit inline styles
- Reports.tsx: downloadError div → AlertBanner; surface token fix self-resolves
- Settings.tsx: AddProjectForm error div → AlertBanner; health banner → StatusDot + Badge
</objective>

<execution_context>
@D:/School/DoAnTotNghiep/chat-system/.claude/get-shit-done/workflows/execute-plan.md
@D:/School/DoAnTotNghiep/chat-system/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/School/DoAnTotNghiep/chat-system/.planning/ROADMAP.md
@D:/School/DoAnTotNghiep/chat-system/.planning/phases/01-ui-ux-overhaul/01-RESEARCH.md
@D:/School/DoAnTotNghiep/chat-system/.planning/phases/01-ui-ux-overhaul/01-PATTERNS.md
@D:/School/DoAnTotNghiep/chat-system/.planning/phases/01-ui-ux-overhaul/01-01-SUMMARY.md

<interfaces>
<!-- Contracts created by plan 01-01 that this plan consumes. -->

From dashboard/src/components/AlertBanner.tsx:
  interface AlertBannerProps {
    type: 'error' | 'success' | 'info' | 'warning';
    message: string;
    onDismiss?: () => void;
    action?: { label: string; onClick: () => void };
  }
  export function AlertBanner({ type, message, onDismiss, action }: AlertBannerProps)

From dashboard/src/components/Badge.tsx:
  type Severity = 'critical' | 'high' | 'medium' | 'low' | 'info';
  type StatusVariant = 'passed' | 'failed' | 'running' | 'queued' | 'warning';
  interface BadgeProps { variant: Severity | StatusVariant | 'neutral'; dot?: boolean; children: React.ReactNode; }
  export function Badge({ variant, dot = false, children }: BadgeProps)

From dashboard/src/components/StatusDot.tsx:
  interface StatusDotProps { status: 'ok' | 'error' | 'warn' | 'info' | 'critical' | 'high' | 'medium' | 'low'; label?: string; size?: number; }
  export function StatusDot({ status, label, size }: StatusDotProps)

From dashboard/src/tokens.css (now defined):
  --surface-1: var(--bg-elev)    ← was undefined, now resolves
  --surface-2: var(--bg-muted)   ← was undefined, now resolves

Chat.tsx current notify call sites (all 7 must be replaced):
  line 6:     import { notify } from '../utils/toast';           → DELETE import
  line 122:   notify.processing('Đang tạo báo cáo…')            → setReportLoading(true)
  line 136:   notify.dismissProcessing() + notify.report()      → setReportLoading(false) + setReportStatus(success)
  line 145:   notify.dismissProcessing() + notify.commandError() → setReportLoading(false) + setReportStatus(error)
  line 184:   notify.commandSuccess(res.message)                 → setCmdStatus({ type: 'success', msg: res.message })
  line 199:   notify.commandError(msg)                           → setCmdStatus({ type: 'error', msg })
  line 242:   notify.commandSuccess(res.message)                 → setCmdStatus({ type: 'success', msg: res.message })
  line 250:   notify.commandSuccess(res.message)                 → setCmdStatus({ type: 'success', msg: res.message })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace all Chat.tsx notify calls with local state + AlertBanner</name>
  <files>dashboard/src/pages/Chat.tsx</files>

  <read_first>
    - dashboard/src/pages/Chat.tsx — read full file to find exact line numbers for each notify call, the LoginOverlay surface token references, and the JSX structure before editing
    - dashboard/src/components/AlertBanner.tsx — confirm the AlertBannerProps interface (created in 01-01)
  </read_first>

  <action>
Make the following changes to dashboard/src/pages/Chat.tsx:

STEP 1 — Remove the notify import (line 6):
  DELETE: `import { notify } from '../utils/toast';`

STEP 2 — Add AlertBanner import at the top of imports (after React imports):
  ADD: `import { AlertBanner } from '../components/AlertBanner';`

STEP 3 — Add two new state variables inside the PageChat() function, near the top with other
useState declarations:

  const [cmdStatus, setCmdStatus] = useState<{ type: 'success' | 'error'; msg: string } | null>(null);
  const [reportStatus, setReportStatus] = useState<{ type: 'success' | 'error'; msg: string; downloadUrl?: string } | null>(null);
  const [reportLoading, setReportLoading] = useState(false);

STEP 4 — Replace handleReport notify calls (lines ~122, 136–137, 145–146):
  REPLACE: `notify.processing('Đang tạo báo cáo…')`
  WITH:    `setReportLoading(true); setReportStatus(null);`

  REPLACE (the success branch — dismiss + report):
    `notify.dismissProcessing();`  AND  `notify.report(() => { ... });`
  WITH:
    `setReportLoading(false);`
    `setReportStatus({ type: 'success', msg: 'Báo cáo đã sẵn sàng', downloadUrl: url });`
  Note: preserve the existing download logic (the `url` variable and download anchor) — just
  replace the notify call with setReportStatus. The action prop of AlertBanner will trigger
  the same download.

  REPLACE (the error branch — dismiss + commandError):
    `notify.dismissProcessing();`  AND  `notify.commandError(String(e));`
  WITH:
    `setReportLoading(false);`
    `setReportStatus({ type: 'error', msg: \`Lỗi tạo báo cáo: \${e}\` });`

STEP 5 — Replace executeCommand notify calls (lines ~184, 199):
  REPLACE: `notify.commandSuccess(res.message)`
  WITH:    `setCmdStatus({ type: 'success', msg: res.message });`

  REPLACE: `notify.commandError(msg)`
  WITH:    `setCmdStatus({ type: 'error', msg });`

STEP 6 — Replace handleApproveConfirm and handleRevokeConfirm notify calls (lines ~242, 250):
  REPLACE: `notify.commandSuccess(res.message)`
  WITH:    `setCmdStatus({ type: 'success', msg: res.message });`
  (both occurrences)

STEP 7 — Add AlertBanner renders in JSX. Find the section after the chat header and before
the .ai-messages div (or equivalent content area). Insert:

  {cmdStatus && (
    <AlertBanner
      type={cmdStatus.type}
      message={cmdStatus.msg}
      onDismiss={() => setCmdStatus(null)}
    />
  )}
  {reportStatus && (
    <AlertBanner
      type={reportStatus.type}
      message={reportStatus.msg}
      onDismiss={() => setReportStatus(null)}
      action={reportStatus.downloadUrl
        ? {
            label: 'Tải xuống',
            onClick: () => {
              const a = document.createElement('a');
              a.href = reportStatus.downloadUrl!;
              a.download = '';
              a.click();
            },
          }
        : undefined}
    />
  )}

STEP 8 — Fix LoginOverlay surface token inline styles:
  Find the modal container div in LoginOverlay with `background: 'var(--surface-1)'`.
  Replace that value with `'var(--bg-elev)'`.

  Find all inputs/textareas in LoginOverlay with `background: 'var(--surface-2)'`.
  Replace each value with `'var(--bg-muted)'`.

  Note: --surface-1 and --surface-2 are now aliased in tokens.css (from 01-01), so this
  step is belt-and-suspenders cleanup, not strictly required for correctness, but it makes
  the code use the canonical token names.

IMPORTANT: Every try/catch block that previously called notify.commandError or
notify.dismissProcessing MUST have a replacement setCmdStatus or setReportStatus call in
its catch branch. Do NOT leave empty catch blocks.
  </action>

  <verify>
    <automated>grep -n "notify\." D:/School/DoAnTotNghiep/chat-system/dashboard/src/pages/Chat.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "notify\." dashboard/src/pages/Chat.tsx` returns zero matches
    - `grep "from '../utils/toast'" dashboard/src/pages/Chat.tsx` returns zero matches
    - `grep "AlertBanner" dashboard/src/pages/Chat.tsx` returns at least 2 matches (import + JSX render)
    - `grep "cmdStatus" dashboard/src/pages/Chat.tsx` returns matches (state declared and rendered)
    - `grep "reportStatus" dashboard/src/pages/Chat.tsx` returns matches (state declared and rendered)
    - `cd dashboard && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Chat.tsx has zero notify.* references; all 7 call sites replaced; AlertBanner renders below chat header for command and report feedback; LoginOverlay uses canonical token names; no TypeScript errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade Reports.tsx, Settings.tsx error displays + audit Vulns/Overview/Pipelines</name>
  <files>
    dashboard/src/pages/Reports.tsx
    dashboard/src/pages/Settings.tsx
    dashboard/src/pages/Vulns.tsx
    dashboard/src/pages/Overview.tsx
    dashboard/src/pages/Pipelines.tsx
  </files>

  <read_first>
    - dashboard/src/pages/Reports.tsx — read full file; find downloadError render (inline div with color: var(--sev-crit-fg)) and the select with var(--surface-2)
    - dashboard/src/pages/Settings.tsx — read full file; find AddProjectForm error div (line ~73) and the health status banner (lines ~115–133)
    - dashboard/src/pages/Vulns.tsx — read full file; find SevChip function (lines ~23–25) to replace with Badge
    - dashboard/src/pages/Overview.tsx — read full file; confirm no notify imports, confirm page-header structure
    - dashboard/src/pages/Pipelines.tsx — read full file; find var(--surface-2) usage (line ~138) and any inline style objects with magic px values
    - dashboard/src/components/AlertBanner.tsx — confirm export before importing in pages
    - dashboard/src/components/Badge.tsx — confirm export before importing in Vulns
    - dashboard/src/components/StatusDot.tsx — confirm export before importing in Settings
  </read_first>

  <action>
Five files, targeted edits only. For each file: read first, identify exact line numbers,
then apply the minimal change described.

--- dashboard/src/pages/Reports.tsx ---

CHANGE 1: Add AlertBanner import:
  ADD: `import { AlertBanner } from '../components/AlertBanner';`
  (after existing imports)

CHANGE 2: Replace the downloadError error div. Find:
  `{downloadError && <div style={{ color: 'var(--sev-crit-fg)', fontSize: 12 }}>{downloadError}</div>}`
  Replace with:
  `{downloadError && (
    <AlertBanner type="error" message={downloadError} onDismiss={() => setDownloadError('')} />
  )}`

CHANGE 3: The `var(--surface-2)` in the select element (line ~87 and ~99) is now resolved
by the token alias added in 01-01. No code change needed here — verify by inspection.
If the inline style reads `background: 'var(--surface-2)'`, leave it as-is (alias resolves it).

--- dashboard/src/pages/Settings.tsx ---

CHANGE 1: Add AlertBanner, StatusDot, Badge imports:
  ADD: `import { AlertBanner } from '../components/AlertBanner';`
  ADD: `import { StatusDot } from '../components/StatusDot';`
  ADD: `import { Badge } from '../components/Badge';`
  (after existing imports)

CHANGE 2: Replace AddProjectForm error div. Find:
  `{error && <div style={{ color: 'var(--sev-crit-fg)', fontSize: 11 }}>{error}</div>}`
  Replace with:
  `{error && <AlertBanner type="error" message={error} onDismiss={() => setError('')} />}`

CHANGE 3: In AddProjectForm, the setError state is typed as `useState('')`. Change to
`useState<string>('')` (or leave as-is if TypeScript infers correctly — just ensure
`setError('')` is called in onDismiss which means the type must accept empty string).

CHANGE 4: In the health status banner section (lines ~115–133), replace the inline dot div
and chip span. Find:
  `<div style={{ width: 10, height: 10, borderRadius: '50%', flexShrink: 0, background: health === 'ok' ? 'var(--sev-low-fg)' : health === 'error' ? 'var(--sev-crit-fg)' : 'var(--fg-4)' }} />`
  Replace with:
  `<StatusDot status={health === 'ok' ? 'ok' : health === 'error' ? 'error' : 'info'} />`

  Find the chip span:
  `<span className={\`chip dot \${health === 'ok' ? 'status-passed' : health === 'error' ? 'status-failed' : 'status-running'}\`} style={{ marginLeft: 'auto', fontSize: 10 }}>`
  (the exact class string may vary — read the file to confirm)
  Replace with:
  `<Badge variant={health === 'ok' ? 'passed' : health === 'error' ? 'failed' : 'running'}>`
  and close with `</Badge>` in place of the existing `</span>`.
  Remove the `style={{ marginLeft: 'auto', fontSize: 10 }}` inline style; add
  `style={{ marginLeft: 'auto' }}` to the Badge wrapper OR a parent div if needed.

--- dashboard/src/pages/Vulns.tsx ---

CHANGE 1: Add Badge import:
  ADD: `import { Badge } from '../components/Badge';`

CHANGE 2: Replace SevChip local function (lines ~23–25):
  BEFORE:
    function SevChip({ sev }: { sev: string }) {
      return <span className={`chip dot sev-${sev}`}>{sev}</span>;
    }
  AFTER:
    function SevChip({ sev }: { sev: string }) {
      return <Badge variant={sev as 'critical' | 'high' | 'medium' | 'low' | 'info'} dot>{sev}</Badge>;
    }

--- dashboard/src/pages/Overview.tsx ---

AUDIT ONLY — no changes expected:
  1. Confirm `grep "notify\." dashboard/src/pages/Overview.tsx` returns zero matches.
  2. Confirm `grep "var(--surface" dashboard/src/pages/Overview.tsx` returns zero matches.
  3. Confirm the page uses `.content`, `.page-header`, `.h1`, `.sub` CSS classes.
  If all three checks pass, no edit needed.
  If any check fails, fix the violation using the same patterns as above.

--- dashboard/src/pages/Pipelines.tsx ---

AUDIT ONLY — the var(--surface-2) reference (line ~138) is now resolved by the token alias
from 01-01. No code changes needed.
  1. Confirm `grep "notify\." dashboard/src/pages/Pipelines.tsx` returns zero matches.
  2. Confirm `grep "var(--surface-1)" dashboard/src/pages/Pipelines.tsx` returns zero matches.
  3. The existing `var(--surface-2)` inline style at line ~138 is acceptable — token resolves.
  If audits pass, no edit needed.

IMPORTANT: After ALL edits in this task, run the build to confirm zero TypeScript errors
across all 5 files.
  </action>

  <verify>
    <automated>cd D:/School/DoAnTotNghiep/chat-system/dashboard && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "notify\." dashboard/src/pages/` returns zero matches across all page files
    - `grep -rn "var(--surface-1)" dashboard/src/pages/ dashboard/src/components/` returns zero matches (--surface-1 only remains defined in tokens.css)
    - `grep "AlertBanner" dashboard/src/pages/Reports.tsx` returns a match (import + render)
    - `grep "AlertBanner" dashboard/src/pages/Settings.tsx` returns a match (import + render)
    - `grep "StatusDot\|Badge" dashboard/src/pages/Settings.tsx` returns matches (both imported + used)
    - `grep "Badge" dashboard/src/pages/Vulns.tsx` returns a match (imported + SevChip uses it)
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>

  <done>All 5 pages have their inline error divs replaced with AlertBanner; Vulns uses Badge in SevChip; Settings health banner uses StatusDot + Badge; Overview and Pipelines confirmed clean; full build passes with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → AlertBanner message prop | Error strings from backend API pass through String(e) before display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | AlertBanner (error messages from API) | accept | Error messages are already surfaced via toasts in the existing code; no new disclosure surface introduced; backend controls what goes into error strings |
| T-01-03 | Tampering | Report download action in AlertBanner | accept | Download URL comes from authenticated API response; same trust model as prior notify.report() call; no new attack surface |
</threat_model>

<verification>
Run from project root:

```bash
# 1. Zero toast/notify references across entire dashboard/src
grep -r "notify\.\|from 'sonner'\|from \"sonner\"\|Toaster" dashboard/src/ --include="*.tsx" --include="*.ts"
# Expected: zero matches

# 2. --surface-1 not used as a var() call in any .tsx file (only defined in tokens.css)
grep -r "var(--surface-1)" dashboard/src/ --include="*.tsx" --include="*.ts"
# Expected: zero matches

# 3. AlertBanner used in Chat.tsx, Reports.tsx, Settings.tsx
grep -rl "AlertBanner" dashboard/src/pages/
# Expected: Chat.tsx, Reports.tsx, Settings.tsx

# 4. Full build clean
cd dashboard && npm run build
# Expected: exits 0, no TypeScript errors
```
</verification>

<success_criteria>
- All 7 Chat.tsx notify call sites replaced with local state + AlertBanner renders
- Reports.tsx downloadError shown via AlertBanner (not inline style div)
- Settings.tsx AddProjectForm error shown via AlertBanner; health banner uses StatusDot + Badge
- Vulns.tsx SevChip uses Badge component
- Overview.tsx and Pipelines.tsx confirmed clean (audit passes)
- Zero grep matches for `notify.` across all page files
- Zero grep matches for `var(--surface-1)` in any .tsx file (only in tokens.css definition)
- `cd dashboard && npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-overhaul/01-02-SUMMARY.md` using the
summary template. Include: files modified, each notify call site replaced, design decisions
(e.g., where AlertBanner was positioned in Chat.tsx JSX), and any deviations from the plan.
</output>
